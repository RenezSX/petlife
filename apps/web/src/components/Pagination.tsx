import { ChevronLeft,ChevronRight } from 'lucide-react'; import type { Pagination as PaginationType } from '../types';
export function Pagination({value,onChange}:{value:PaginationType;onChange:(page:number)=>void}){return <div className="pagination"><span>{value.total} registro{value.total===1?'':'s'}</span><div><button className="icon-button" disabled={value.page<=1} onClick={()=>onChange(value.page-1)}><ChevronLeft/></button><strong>{value.page} de {value.totalPages}</strong><button className="icon-button" disabled={value.page>=value.totalPages} onClick={()=>onChange(value.page+1)}><ChevronRight/></button></div></div>}
